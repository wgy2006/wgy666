"""Storage adapters.

The current implementation is intentionally in-memory so the foundation runs
without extra services. A PostgreSQL adapter can later implement the same
methods and be injected at the API/service boundary.
"""
