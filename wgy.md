# AI Prompts Log

## 2026-07-06

用户需求：

> 我们要实现一个github issue分析平台，这是个大项目，目前我们先要实现后续所有功能的基石：搭建一个能从github链接拉项目的各类信息并做好分类的python后端，以及一个简陋的展示这些信息的React前端，这会作为后续小组成员开发的基石和框架，所以确保可拓展性即可，开始干吧

补充约束：

> 现在项目里只有文档，其实就是空的，不用看文档就按我说的要求，直接从零开始放开手脚干即可，python用uv，react用npm

本轮实现决策：

- 后端使用 `uv + FastAPI + httpx + pydantic-settings`。
- 前端使用 `npm + React + Vite + TypeScript`。
- 当前先用内存存储，避免过早引入 PostgreSQL、Redis、Celery 的部署成本。
- GitHub 数据同步先覆盖仓库元信息、README、语言、目录树、Issue、PR、提交记录。
- 分类先使用可解释规则，后续可扩展为规则初筛、LLM 复核、RAG 上下文增强。

## 2026-07-07

用户需求：

> 我负责第三部分：项目解析功能（无 AI 部分），比如项目架构思维导图之类的自动化解析展示图。本周是界面原型迭代，不是技术原型迭代。请完成第三部分这周应该完成的部分，先不要提交 Git。

实现约束：

- 本轮只做“界面原型迭代”，不提前做真实后端解析服务。
- 第三部分负责项目解析展示，数据来源复用成员1已经同步到前端的仓库快照。
- 不接 AI，不做 RAG，不做深度代码语义理解。
- 需要在 Markdown 中记录本轮提示词和实现思路，方便组员或后续 AI 查看。

本轮实现决策：

- 在前端基于 `RepositorySnapshot.files`、`file_categories`、`languages` 和 `readme` 做规则化展示。
- 增加“项目解析原型”面板，展示项目类型推断、源码/依赖/测试/文档数量、主要目录、依赖文件、入口文件候选。
- 增加一个静态项目架构思维导图，用于评审时说明项目解析功能的界面形态。
- 后端暂不新增接口，真实解析服务、依赖文件内容解析和更准确的架构图放到技术原型迭代。

## 2026-07-13 至 2026-07-14

用户需求：

> 完善“项目结构概览”前端。点击“主要目录”“依赖文件”等组件后，需要进入另一个页面，把后续计划实现的项目架构思维导图和自动化解析结果完整展示出来。当前知识库还没有完全接通，可以先使用示例状态，但页面结构要完整，并参考组内其他模块的现有样式。

界面调整：

> 四个入口卡片中的标题需要统一顶端对齐，使卡片排列更整齐。

本轮实现决策：

- 保持现有三栏工作区、蓝绿配色和卡片样式，不新增路由库。
- “项目结构概览”中的架构图节点和四个信息卡片均改为可点击入口。
- 在中间工作区增加架构图、主要目录、依赖文件、入口文件、测试与文档五个详情视图，并提供统一返回入口和标签切换。
- 优先展示本次仓库同步得到的真实文件统计；数据不足时使用明确标注的“原型示例”，避免把示例数据误认为真实解析结果。
- 架构图展示仓库、核心源码、依赖与运行、工程支撑之间的关系，同时补充目录职责、依赖清单、启动链路和质量保障流程。
- 页面切换时自动回到顶部，详情页在三栏布局和手机宽度下自动改为单栏，避免内容遮挡和横向溢出。
- 四个入口卡片统一采用纵向顶部对齐，标题、图标和箭头保持在同一高度。

后续数据接入：

- 当前主分支已经包含 PostgreSQL 仓库持久化、知识图谱构建、知识块与向量检索代码。
- 后续将详情页中的示例模块关系替换为知识图谱节点与边，将依赖、入口和测试链路替换为后端解析接口返回的真实结果。

## 2026-07-17

用户需求：

> 在 `wgy` 分支上补齐技术原型迭代进度，不影响 `main`。重点完善成员 3 负责的非 AI 项目解析，使前端结构视图不再依赖固定示例数据。

本轮实现决策：

- 将 `wgy` 快进到最新 `main`，保留其他成员已合入的仓库同步、智能问答和 Issue 分析功能。
- 项目结构概览在仓库同步后调用后端 `project-structure` 接口；接口暂不可用时才使用同步快照生成本地降级结果，并在界面中明确标识数据来源。
- 后端合并目录树样本与已索引文件，去重后统计源码、测试、文档、配置、CI/CD、主要目录和入口文件。
- 优先保留依赖与配置清单，解析 `package.json`、`pyproject.toml`、`requirements*.txt` 和 `Cargo.toml`，返回依赖分组与已识别框架。
- 详情页移除固定目录、依赖、入口和工程文件示例；没有真实结果时显示明确空状态。
- 修复知识图谱中文检索把 `TypeScript` 误判为 `test` 和 `script` 的问题，改为按词项匹配。
- 增加项目解析服务、依赖解析和缓存接口测试，并在提交前执行后端全量测试、前端 lint/build 和浏览器联调。

界面回归修复：

> 依赖分组页面中的仓库路径超出了“依赖清单”卡片。

- 原因是 Grid 子项保留了长路径的最小内容宽度，路径文本也没有启用强制换行。
- 为依赖卡片增加 `min-width: 0`，并允许长路径和 scoped package 名称在容器内换行，不改变页面结构和配色。

目录树可视化优化：

> 在不改变现有页面结构和蓝绿配色的前提下，美化“主要目录”详情，把普通目录列表改成类似思维导图的展示。

- 保留原有项目解析详情页和左右信息分区，不引入新的前端依赖。
- 将目录列表改为“仓库根节点 - 分支连线 - 主要目录节点”的思维导图结构，节点数据继续使用后端项目解析接口的真实结果。
- 目录节点展示职责说明、文件数量、文件类别和源码数量；点击节点后，右侧对应目录职责同步高亮。
- 后端无目录结果时显示明确空状态，不使用固定示例补齐数据。
- 桌面端保留横向树状连线，手机端自动切换为纵向节点并隐藏连线，避免内容拥挤和横向溢出。
- 提交前执行前端 lint、生产构建，并通过浏览器检查桌面端、手机端、节点交互和控制台状态。

目录图对齐修正：

> 目录思维导图在双栏页面中被压得过窄，目录名、职责和统计徽标连续换行，导致节点高度不一致、根节点位置下沉，需要重新整理布局。

- 限制横向思维导图根节点的最大宽度，把更多空间留给目录分支。
- 目录节点统一为固定高度，职责摘要最多显示两行，并减少重复统计徽标，避免内容撑高卡片。
- 根据目录面板自身宽度切换布局：空间不足时改为根节点在上、目录分支在下的纵向树，而不是继续压缩节点。
- 右侧职责卡片统一最小高度和标题/说明列宽，使左右两组内容按行对齐。
- 浏览器实测六个目录节点高度均为 92px，手机端无横向溢出，控制台无错误。

详情面板与依赖关系图优化：

> “主要目录”与“测试和文档”页面的左右外层面板底部没有齐平；依赖分组仅为普通文字列表，无法清晰表达依赖清单与各类依赖之间的关系。

- 只对项目解析模块中的目录详情和质量详情布局启用等高拉伸，不修改其他成员页面的通用布局。
- 目录详情左右面板浏览器实测高度均为 814px；质量详情左右面板高度均为 462px。
- 将依赖分组改为“依赖清单根节点 - 连接线 - 依赖类别分支”的关系视图。
- 依赖根节点展示声明文件数量、已解析依赖总数和清单文件；分支节点展示类别图标、类别数量和具体依赖标签。
- 宽屏保留关系连线，窄屏和手机端自动改为纵向布局；数据仍全部来自后端依赖解析结果，不添加固定示例。

## 2026-07-18

用户需求：

> 基于最新 `main` 在 `wgy-try` 分支大幅优化整个项目前端，可以调整较多界面，但尽量不要修改其他成员实现的后端、数据库和接口逻辑。

本轮实现决策：

- 用最新 `origin/main` 重新建立本地 `wgy-try` 基线，保留队友刚合入的 GitHub Token 克隆、Webhook 异步处理和 LLM 错误处理，不修改后端与数据库文件。
- 保留原有“左侧仓库同步、中间仓库工作台、右侧 Repository Agent”三栏结构和蓝绿配色，重点重做信息层级与组件一致性。
- 建立统一的颜色、边框、阴影、圆角、焦点和状态变量，让仓库卡片、指标、列表、弹窗、项目解析与聊天区域采用同一套视觉规范。
- 左侧增加仓库上下文状态、折叠式同步范围和工作台导航；顶部增加面包屑、前端状态和统一操作按钮。
- 仓库概览增加安全上下文、默认分支、同步时间和 GitHub 入口，指标卡、README、活动列表和文件浏览统一交互反馈。
- Issue 区域增加“事件接入、自动分类、回复建议、修复候选”四阶段状态视图，数据只由现有快照和 Webhook 事件推导，不新增接口。
- Repository Agent 增加上下文状态、快捷问题、来源提示和新的输入区，继续使用原有问答接口和引用数据。
- 空状态改为同步、解析、协作三步引导；桌面、窄屏和手机端分别调整布局，手机端无横向溢出。
- `vite.config.ts` 改为 `VITE_API_BASE_URL` 环境变量优先、现有服务器地址兜底，部署默认行为不变，本地联调无需修改受版本控制的服务器地址。

验证结果：

- 前端 lint 与生产构建通过。
- 使用本地后端同步 `fastapi/fastapi`，仓库概览、项目解析、Issue 流水线和聊天上下文均能显示真实数据。
- 1280px 桌面宽度和 390px 手机宽度均无横向溢出，浏览器控制台无错误。

交互修正：

- 左侧四个工作台入口改为受控导航按钮，激活颜色随用户点击切换，不再固定高亮“仓库概览”。
- “项目解析”直接进入项目架构详情，“Issue 智能分析”定位到 Issue 面板。
- “仓库问答”在桌面端高亮右侧 Agent 并聚焦输入框，在窄屏设备上同时滚动到问答区域。
- 尚未同步仓库时点击数据相关入口，会聚焦仓库地址输入框，避免无反馈。
